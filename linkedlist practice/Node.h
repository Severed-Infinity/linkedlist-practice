/*
#ifndef NODE_H
#define NODE_H

template<class T>
class Node {
	//friend const LinkedList;
public:
	Node();
	Node(const T& data, Node<T>* ptrnext = NULL) : data(data), next(ptrnext){}
	~Node();
	T data;
	Node<T>* next;
private:

};

#endif
*/